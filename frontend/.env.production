# Empty value: the React app falls back to '' and uses relative URLs like /api/todos
# nginx.conf inside the frontend container proxies /api/ to the backend service.
# Do NOT put http://localhost:5001 here — Create React App bakes this value into the
# JS bundle at BUILD time, so a wrong value here breaks the deployed site.
REACT_APP_API_URL=
